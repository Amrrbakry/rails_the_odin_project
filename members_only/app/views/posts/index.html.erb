<%= provide(:title, "All posts") %>
<h4>Members Only is an exclusive clubhouse where members can write embarrassing posts about non-members. </h4>

<h1>All posts </h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  	<% if logged_in? %>
  	<% else %>
  		<span id="login_link">
  			<%= link_to "Login to write a post", new_post_path %>
  		</span>
  	<% end %>
  	<%= render @posts %>
  </div>
</div>