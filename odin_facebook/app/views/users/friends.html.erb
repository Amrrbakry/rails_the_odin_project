<div class="col-md-offset-3 col-md-6">
	<h2>Your friends</h2>
	<div class="all-users" id="friends">
		<% if @friends.any? %>
			<h3> <%= pluralize(@friends.count, "friend") %>  </h3> 
			<hr> 
			<%= render @friends %>
		<% else %>
			<h3>You have no friends :( </h3>
		<% end %>
	</div>
</div>