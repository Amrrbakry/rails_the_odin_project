<div class="col-md-offset-3 col-md-6">
	<h2>Notifications</h2>
	<div class="all-users" id="notifications">
	<% if @pending_requests.any? %>
		<%= render @pending_requests %>
	<% else %>
	<h3> No new notifications. </h3>
	<% end %>
	</div>
</div>