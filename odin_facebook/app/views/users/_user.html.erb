<ul id="all-users-list">
	<li>
		<h3> 
		<%= gravatar_for user %>
		<%= link_to user.username.titleize, user %> 
		</h3>
		<h5> <%= user.email %> </h5>
		<h5 class="text-right"> 
			<%= render partial: 'friend_form', locals: { user: user } %>
		</h5>
	</li>
	<hr>
</ul>